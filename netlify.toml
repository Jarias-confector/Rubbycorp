[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA: todas las rutas se resuelven en el cliente.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
